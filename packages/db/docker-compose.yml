name: selimsolutions

services:
  mysql:
    image: mysql
    container_name: selimsolutions-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: selimsolutions
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"
    volumes:
      - selimsolutions_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  selimsolutions_mysql_data:
